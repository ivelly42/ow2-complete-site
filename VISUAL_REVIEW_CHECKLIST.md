# 오버워치 아트풍 구현 리뷰 체크리스트

기준일: 2026-04-23

## 사용 원칙
- 엔지니어는 PR 전 자기 검수에 이 체크리스트를 사용한다.
- UXDesigner는 같은 기준으로 승인/수정 요청을 판단한다.
- `필수` 항목 하나라도 실패하면 승인하지 않는다.

## 판정 기준
- `P0`: 브랜드 인상 또는 정보 위계가 깨진 상태. 즉시 수정 필요.
- `P1`: 화면은 동작하지만 폴리시 밀도가 부족한 상태. 되도록 같은 PR에서 수정.
- 승인 조건은 `P0` 0건, `P1` 최대 1건이다.

## 1. 첫 화면 인상
- `필수 / P0` 첫 화면이 정보 카드 묶음이 아니라 포스터형 히어로 화면으로 읽힌다.
- `필수 / P0` 사용자는 첫 화면에서 "이 사이트가 무엇인지"와 "지금 봐야 할 변화"를 바로 파악할 수 있다.
- `필수 / P0` 히어로 영역 안에 포컬 레이어가 있다. 캐릭터 아트가 없더라도 사선 프레임, 라이트 스윕, 강조 레인이 비어 있지 않다.
- `필수 / P0` 히어로보다 아래 섹션이 더 강하게 눈에 들어오지 않는다.

## 2. 타이포 역할
- `필수 / P0` `Black Han Sans`는 큰 한국어 제목과 결정 문장에만 쓰인다.
- `필수 / P0` `Barlow Condensed`는 숫자, 시즌명, 버튼, 필터, 배지, 영문 라벨에 쓰인다.
- `필수 / P0` 긴 설명과 목록은 `Noto Sans KR`로 읽기 편하게 유지된다.
- `필수 / P1` 날짜, 패치 기준, 출처 메타는 `JetBrains Mono`로 구분된다.
- `필수 / P1` 한 컴포넌트 안에서 서로 다른 폰트 역할이 뒤섞여 위계가 흐려지지 않는다.

## 3. 형태 언어
- `필수 / P0` `999px` 완전 pill 형태가 작은 배지 외의 기본 모양이 되지 않는다.
- `필수 / P0` 히어로 또는 강조 카드 중 최소 한 곳에서 사선 컷, 프레임 오버레이, 엣지 라인이 보인다.
- `필수 / P1` 같은 패밀리의 카드는 라운드, 보더 두께, 표면 계층이 일관된다.
- `필수 / P1` 모든 카드가 서로 다른 장식 문법을 쓰지 않는다.

## 4. 재질감과 색 사용
- `필수 / P0` 오렌지와 블루가 같은 컴포넌트에서 동시에 주연 발광으로 싸우지 않는다.
- `필수 / P0` 글래스/블러 패널은 같은 섹션에서 한두 개 이하로 제한된다.
- `필수 / P1` 메탈릭은 테두리/엣지 위주로 쓰이며, 면 전체를 금속처럼 채우지 않는다.
- `필수 / P1` 배경 질감은 존재하되 텍스트 가독성을 해치지 않는다.

## 5. 정보 리듬
- `필수 / P0` 각 섹션에는 주연 정보 1개와 조연 정보 그룹이 분명히 나뉜다.
- `필수 / P0` 시즌/주의 메모/핵심 변화가 본문 카드들 사이에 묻히지 않는다.
- `필수 / P1` 출처 섹션은 신뢰를 주되, 히어로/핵심 요약보다 더 화려하지 않다.
- `필수 / P1` 스크롤이 내려갈수록 포스터 감도에서 정보 시스템 감도로 자연스럽게 전환된다.

## 6. 인터랙션과 모션
- `필수 / P1` 무한 모션은 한 뷰포트 안에서 한 종류 이하다.
- `필수 / P1` hover/focus는 120~360ms 안에서 짧고 명확하게 끝난다.
- `필수 / P1` 바운스, 둥실거림, 반복 펄스처럼 캐주얼한 모션은 없다.

## 7. 반응형
- `필수 / P0` 모바일 첫 화면에서도 여전히 브랜드 인상이 살아 있다.
- `필수 / P0` 모바일에서 CTA, 제목, 핵심 신호가 첫 구간에 정리되어 보인다.
- `필수 / P1` 장식 프레임이나 광원 때문에 텍스트/버튼이 잘리거나 읽기 어려워지지 않는다.
- `필수 / P1` 카드가 1열 또는 2열로 재배치돼도 주연/조연 위계가 유지된다.

## 8. 바로 반려해야 하는 레드 플래그
- 내비게이션, CTA, 필터, 배지가 모두 같은 조형 계열로 처리됐다.
- 제목, 숫자, 버튼, 메타 라벨이 사실상 같은 폰트 톤으로 보인다.
- 첫 화면보다 소스/출처 카드가 더 눈에 띈다.
- 블러 글래스 카드가 한 섹션에 여러 개 반복된다.
- 오렌지 글로우와 블루 글로우가 한 카드 안에서 경쟁한다.
- 모바일 첫 화면이 포스터가 아니라 일반 대시보드처럼 보인다.

## 9. 현재 구현에 바로 대입할 확인 포인트
- [src/index.css](/Users/chanho/.paperclip/instances/default/projects/14f24d20-b25a-4794-8c8c-3a0013a0cc11/8798f8d1-e5b3-4d87-8800-781d09194a8c/_default/src/index.css:1)의 폰트 토큰은 `DESIGN.md` 역할 분리와 일치해야 한다.
- [src/App.css](/Users/chanho/.paperclip/instances/default/projects/14f24d20-b25a-4794-8c8c-3a0013a0cc11/8798f8d1-e5b3-4d87-8800-781d09194a8c/_default/src/App.css:1)의 내비게이션/CTA/필터는 모두 같은 각진 컨트롤 조형으로 수렴하지 않게 다음 라운드에서 보완해야 한다.
- 히어로 패널은 지금도 강한 시작점이지만, 캐릭터 포컬 레인을 더 명확히 남기는 방향으로 확장해야 한다.

## 10. 리뷰 코멘트 템플릿
### 승인
- 폰트 역할 분리 통과
- 히어로 포컬 레이어 통과
- 카드 패밀리 일관성 통과
- 모바일 첫 화면 인상 통과

### 수정 요청
- `P0`: 어떤 항목이 브랜드 인상 또는 정보 위계를 깨는지 명확히 적는다.
- `P1`: 이번 PR에서 같이 고칠지, 다음 라운드로 넘길지 우선순위를 적는다.
