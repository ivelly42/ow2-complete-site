# 오버워치 아트풍 핵심 화면 비주얼 폴리시

기준일: 2026-04-23

적용 범위
- 현재 단일 페이지 구현([src/App.tsx](/Users/chanho/.paperclip/instances/default/projects/14f24d20-b25a-4794-8c8c-3a0013a0cc11/8798f8d1-e5b3-4d87-8800-781d09194a8c/_default/src/App.tsx:1))의 홈 화면 전체
- 이후 추가될 시즌 상세, 영웅 상세, 패치/출처 확장 화면의 공통 기준

## 1. 이 문서의 목적
- `DESIGN.md`의 방향을 실제 화면 승인 규칙으로 잠근다.
- 첫 화면은 포스터처럼 강하게, 아래 섹션은 정보 허브처럼 명확하게 읽히게 한다.
- "오버워치풍"을 네온 과장으로 해석하지 않고, 캐릭터 중심성 + 작전 브리핑 구조감 + 경기장 에너지의 조합으로 해석한다.

## 2. 화면 우선순위
1. 히어로 영역은 브랜드 인상, 현재 시즌 온도, 핵심 행동 유도를 동시에 책임진다.
2. 첫 스크롤 구간의 요약 카드는 "지금 가장 중요한 변화" 1개와 "운영상 주의 메모" 1개만 강하게 잡는다.
3. 시즌/서브롤/영웅/모드/경쟁전 섹션은 읽기 쉬운 정보 시스템으로 정리하되, 같은 템플릿 반복처럼 보이면 안 된다.
4. 출처 섹션은 신뢰를 보강하지만 시각 주도권은 가져가면 안 된다.

승인 원칙
- 첫 화면보다 아래 섹션이 더 화려하면 실패다.
- 사용자는 3초 안에 "오버워치2 정보 허브"와 "지금 달라진 점"을 동시에 파악해야 한다.

## 3. 히어로 영역 포컬 레이어
히어로 영역은 아래 4개 레이어를 기준으로 설계한다.

1. 배경 대기층
- 차콜 기반 배경 위에 오렌지/블루 광원을 얇게 겹친다.
- 전체 화면을 밝게 만들지 말고, 프레임 안쪽에 열기가 모이는 느낌으로 제한한다.

2. 프레임/작전 오버레이층
- 사선 컷, 금속 엣지, 얇은 라인 오버레이를 사용한다.
- 수평/수직 직사각형만으로 끝내지 않는다.
- 첫 화면에서는 프레임 오버레이가 반드시 한 번은 보여야 한다.

3. 캐릭터 포컬층
- 데스크톱 기준 우측 35~45% 폭을 캐릭터 또는 캐릭터를 대체하는 포컬 에너지 레이어에 배정한다.
- 실제 캐릭터 아트가 들어오면 얼굴과 상체가 첫 뷰포트 상단 1/3 안쪽에 걸리게 한다.
- 캐릭터 아트가 아직 없으면 같은 영역을 비워 두지 말고, 라이트 스윕과 사선 프레임으로 "캐릭터가 들어올 자리"를 분명히 남긴다.

4. 정보/행동층
- 좌측 45~55% 폭에 제목, 한 줄 요약, CTA를 배치한다.
- 첫 화면에서 동시에 강하게 보이는 요소는 `제목`, `현재 기준선 한 줄`, `주 CTA`까지 3개를 넘기지 않는다.

## 4. 타이포 역할 규칙
### `Black Han Sans`
- 한국어 대제목, 섹션의 결정 문장, 브랜드 인상을 책임지는 큰 카피에만 사용한다.
- 버튼, 필터, 배지, 짧은 메타 라벨에는 사용하지 않는다.
- 한 화면에서 과하게 반복하면 포스터 힘이 사라지므로 "크게 한 번" 쓰는 용도로 제한한다.

### `Barlow Condensed`
- 시즌 번호, 수치, 통계, 버튼, 필터, 배지, 영문 UI 라벨, 영문 영웅명에 사용한다.
- 영어/숫자 리듬을 만드는 역할이므로 짧고 강한 단위에서만 쓴다.
- 긴 한국어 문단에 사용하지 않는다.

### `Noto Sans KR`
- 본문, 카드 설명, 목록, 운영 메모, 긴 정보 텍스트의 기본값이다.
- 화면 밀도가 올라가는 중하단 섹션에서 가독성을 책임진다.

### `JetBrains Mono`
- 날짜, 패치 버전, 출처 메타, 검증 메모에 사용한다.
- 장식이 아니라 "검증 가능한 정보"라는 인상을 주는 용도다.

추가 원칙
- 한 컴포넌트 안에서 역할이 다른 폰트를 3개 이상 섞지 않는다.
- 같은 줄 안에서 `Black Han Sans`와 `Barlow Condensed`를 무분별하게 섞지 않는다. 분리가 필요하면 줄을 나누거나 강조 덩어리를 분리한다.

## 5. 카드 패밀리와 형태 언어
카드는 아래 4개 패밀리로 구분한다.

### A. 히어로 패널
- 가장 큰 장식 허용 구간이다.
- 프레임 오버레이, 라이트 스윕, 메탈 라인을 적극 사용한다.
- 유리 질감은 히어로 내부 보조 패널 1개까지만 허용한다.

### B. 강조 카드
- 현재 시즌 기준선, 운영상 주의 메모처럼 꼭 먼저 읽어야 하는 카드다.
- 카드 자체가 화면을 끌어당길 수 있지만, 첫 화면 안에서 2개 이상 동시에 주연이 되면 안 된다.
- 대각선 광원, 금속 엣지, 오렌지 또는 워닝 계열 하이라이트를 쓸 수 있다.

### C. 데이터 카드
- 시즌 타임라인, 서브롤, 모드, 경쟁전, 영웅 로스터용 카드다.
- 역할은 "읽기 쉬움"이 우선이다.
- 배경은 안정적으로 유지하고, 강조는 상단 라벨이나 하단 바처럼 얇게 준다.

### D. 출처 카드
- 신뢰를 보강하는 카드다.
- 형태 언어는 유지하되, 강조도는 데이터 카드보다 낮아야 한다.
- CTA가 들어가더라도 "원문 열기"가 주 CTA처럼 보이면 안 된다.

형태 언어 공통 기준
- 기본 라운드는 12~18px을 사용하고, 큰 패널은 18~28px까지 허용한다.
- `999px` 완전 pill 형태는 작은 배지와 상태칩에만 사용한다.
- 내비게이션, 필터, CTA, 주요 카드가 모두 동일한 pill로 보이면 브랜드성이 약해지므로 승인하지 않는다.
- 최소 한 군데는 사선 컷 또는 프레임 오버레이가 보여야 하며, 동시에 모든 카드가 잘려 있으면 과하다.

## 6. 메탈릭 재질감과 발광/글래스 한계선
메탈릭
- 메탈은 면 전체를 채우는 재질이 아니라 테두리, 디바이더, 배지, 프레임 엣지에만 사용한다.
- 금속감은 노란 금색보다 톤다운된 `Metal` 계열 반사로 처리한다.

발광
- 강한 발광은 컴포넌트당 1색만 주연으로 사용한다.
- 오렌지와 블루를 동시에 강하게 켜서 싸우게 만들지 않는다.
- 발광은 "클릭 가능한 주 CTA"나 "현재 활성 상태"를 띄우는 용도에서만 사용한다.

글래스
- 글래스/블러는 히어로 내부 대시보드나 정보 오버레이처럼 깊이를 주는 위치에만 쓴다.
- 같은 섹션에서 여러 카드가 동시에 블러 유리를 쓰면 generic 게임 대시보드처럼 보이므로 금지한다.

배경
- 배경은 평평한 단색으로 끝내지 않되, 텍스트 대비를 해칠 정도의 패턴/노이즈를 올리지 않는다.
- 정보 섹션 배경은 히어로보다 조용해야 한다.

## 7. 모션 규칙
- 기본 이징은 `cubic-bezier(0.16, 1, 0.3, 1)`을 유지한다.
- 진입 모션은 패널 슬라이드와 스태거 리빌 위주로 사용한다.
- 무한 모션은 첫 화면의 라이트 스윕처럼 "한 번에 한 종류"만 허용한다.
- 둥실거림, 바운스, 계속 커졌다 작아지는 펄스는 금지한다.
- hover는 1~2px 상승이나 보더/광원 변화 정도로 제한한다.

## 8. 반응형 규칙
- 모바일에서도 첫 화면은 여전히 포스터처럼 보여야 한다.
- 작은 화면에서는 장식을 모두 제거하는 대신, 사선 프레임 한 개와 주 CTA 한 개는 남긴다.
- 장식 요소가 줄어들더라도 타이포 역할 분리는 유지한다.
- 모바일 첫 화면이 "pill 버튼 묶음 + 카드 나열"처럼 시작하면 실패다.

## 9. 현재 구현 기준 즉시 고정할 판단
- 타이포 역할 분리는 코드에서 먼저 잠근다. `Black Han Sans`는 한국어 결정 문장, `Barlow Condensed`는 숫자/버튼/라벨로 분리한다.
- 현재 [src/App.css](/Users/chanho/.paperclip/instances/default/projects/14f24d20-b25a-4794-8c8c-3a0013a0cc11/8798f8d1-e5b3-4d87-8800-781d09194a8c/_default/src/App.css:1) 기준으로 내비게이션, CTA, 필터가 비슷한 각진 컨트롤 레시피로 수렴하는 경향이 있다. 다음 구현 라운드에서 우선 줄여야 한다.
- 히어로 영역은 현재도 프레임과 스윕이 있지만, 실제 캐릭터가 들어올 포컬 레인이 더 분명해져야 한다.
- 중하단 카드의 정보 구조는 안정적이므로, 다음 수정은 카드 자체를 더 화려하게 만드는 것보다 "주연 카드와 조연 카드 차이"를 키우는 쪽이 우선이다.
