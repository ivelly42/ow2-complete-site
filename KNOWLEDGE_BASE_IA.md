# 오버워치2 지식 베이스 IA 및 탐색 구조 설계

기준일: 2026-04-23

연결 문서
- `DESIGN.md`
- `V2_KNOWLEDGE_BASE_PLAN.md`
- `CORE_SCREEN_VISUAL_POLICY.md`
- `VISUAL_REVIEW_CHECKLIST.md`

## 1. 이 문서의 목적
- 현재 단일 페이지 구성을 `탐색 가능한 지식 베이스`로 전환하기 위한 정보 구조와 탐색 규칙을 고정한다.
- 홈은 포스터형 허브로 유지하고, 인덱스와 상세는 정보 시스템처럼 읽히게 역할을 분리한다.
- 사용자가 `무엇이 달라졌는지`, `어디서 찾아야 하는지`, `지금 정보가 얼마나 믿을 만한지`를 빠르게 파악하게 만든다.

## 2. 판단 기준과 우선순위
### P0
- 핵심 정보 축마다 전용 진입 페이지가 있다.
- 상세 페이지는 반드시 상위 인덱스, 관련 항목, 공식 출처로 되돌아갈 수 있다.
- 같은 사실을 홈과 상세에 중복 복사하지 않는다.
- 모든 상세 페이지는 `절대 날짜`, `출처 수`, `검증 상태`를 노출한다.

### P1
- 인덱스 페이지는 필터와 정렬만으로 주요 탐색 과제를 해결한다.
- 시즌, 패치, 맵처럼 시간이 흐르며 쌓이는 축은 아카이브형 구조로 읽힌다.
- 모바일에서도 현재 위치와 상위 경로를 잃지 않는다.

### P2
- 전역 검색이나 빠른 점프 같은 탐색 가속 장치를 붙일 자리를 미리 남긴다.
- 관련 콘텐츠 모듈이 "다음에 무엇을 봐야 하는가"를 제안한다.

## 3. 사용자 목표
1. 복귀 유저는 홈에서 최근 변화의 기준선을 30초 안에 파악해야 한다.
2. 신규 유저는 영웅, 역할, 모드 구조를 헷갈리지 않고 배울 수 있어야 한다.
3. 기존 유저는 시즌/패치/맵 변화만 빠르게 찾아 들어갈 수 있어야 한다.
4. 운영자는 다음 패치 때 어느 엔티티를 갱신하면 되는지 즉시 알아야 한다.

## 4. 설계 원칙
- 홈은 요약하고 상세는 설명한다.
- 인덱스는 넓게 훑게 하고, 상세는 깊게 읽게 한다.
- 모든 상세 페이지는 최소 두 경로로 도달 가능해야 한다.
  한 번은 전역 내비게이션에서, 한 번은 관련 콘텐츠 링크에서 들어갈 수 있어야 한다.
- 탐색은 "예쁜 카드 나열"이 아니라 "정보 축별 문서 체계"로 느껴져야 한다.
- 브랜드 강도는 홈에서 가장 높고, 인덱스/상세로 갈수록 차분해져야 한다.

## 5. 권장 사이트맵
```text
/
|- /heroes
|  |- /heroes/:heroSlug
|- /seasons
|  |- /seasons/:seasonSlug
|- /patches
|  |- /patches/:patchSlug
|- /modes
|  |- /modes/core-pvp
|  |- /modes/competitive
|  |- /modes/stadium
|- /maps
|  |- /maps/:mapSlug
|- /sources
```

URL 규칙
- URL은 안정성과 공유 편의를 위해 영문 슬러그를 사용한다.
- 패치 슬러그는 날짜 기반으로 고정한다.
  예: `/patches/2026-04-17`
- 시즌 슬러그는 연도와 시즌명을 함께 가진다.
  예: `/seasons/2026-season-2-summit`
- UI 라벨은 전부 한글로 유지한다.

## 6. 페이지 계층 정의
| 페이지 | 목적 | 주요 모듈 | 탐색 역할 |
| --- | --- | --- | --- |
| 홈 `/` | 현재 기준선과 각 지식 축 진입 | 포스터형 히어로, 최신 변화, 진입 카드, 워치리스트 요약 | 가장 넓은 진입 허브 |
| 영웅 인덱스 `/heroes` | 영웅 전체를 역할과 상태로 찾기 | 역할 필터, 서브롤 설명, 영웅 카드 그리드 | 탐색형 인덱스 |
| 영웅 상세 `/heroes/:heroSlug` | 개별 영웅의 현재 맥락 이해 | 한 줄 요약, 역할/서브롤, 시즌 메모, 주의 포인트, 관련 모드/패치/출처 | 학습형 상세 |
| 시즌 인덱스 `/seasons` | 연간 흐름과 시즌 아크 확인 | 연도 필터, 시즌 타임라인, 시즌 카드 | 시간축 허브 |
| 시즌 상세 `/seasons/:seasonSlug` | 해당 시즌의 핵심 변화 묶음 | 시즌 개요, 주요 추가 요소, 연결 패치, 영향받은 영웅/모드/맵 | 변곡점 문서 |
| 패치 인덱스 `/patches` | 절대 날짜 기준으로 변경 내역 추적 | 연도/월 필터, 최신순 리스트, 충돌 표시 | 운영형 아카이브 |
| 패치 상세 `/patches/:patchSlug` | 특정 패치의 사실 기준선 확인 | 변경 요약, 영향 엔티티, 충돌 메모, 출처 | 업데이트 기준 문서 |
| 모드 허브 `/modes` | 코어 PvP, 경쟁전, Stadium 구분 | 모드 카드, 핵심 규칙 비교, 관련 시즌 링크 | 분류형 허브 |
| 모드 상세 `/modes/*` | 모드별 규칙과 현재 상태 설명 | 규칙, 현재 운영 상태, 관련 맵/패치/영웅 | 시스템 문서 |
| 맵 인덱스 `/maps` | 맵 전체와 리워크 상태 탐색 | 모드 필터, 리워크 배지, 맵 카드 | 아카이브 인덱스 |
| 맵 상세 `/maps/:mapSlug` | 맵의 소속, 리워크, 연관 패치 확인 | 맵 개요, 관련 모드, 리워크 기록, 출처 | 변화 추적 문서 |
| 출처 `/sources` | 공식 링크, 최신성 기준, 충돌 메모 확인 | 출처 목록, 검증 정책, 워치리스트 | 신뢰 허브 |

## 7. 전역 탐색 모델
### 상단 전역 내비게이션
- 1차 메뉴는 `홈 / 영웅 / 시즌 / 패치 / 모드 / 맵 / 출처`로 고정한다.
- 데스크톱에서는 상단에 항상 보이고, 현재 섹션은 텍스트와 언더라인으로만 강조한다.
- 모바일에서는 메뉴 시트로 접되, 상단에 현재 기준일 칩이나 `업데이트 기준` 링크 하나는 남긴다.

### 공통 헤더 유틸리티
- 전역 우측 영역에는 `마지막 검증 기준일`과 `출처 정책` 진입점을 둔다.
- 검색은 P0 필수는 아니지만, 이후 빠른 점프를 넣을 수 있도록 헤더 우측 슬롯을 비워 둔다.

### 브레드크럼
- 홈을 제외한 모든 상세 페이지는 브레드크럼을 가진다.
- 기본 구조는 `홈 > 섹션 인덱스 > 상세`다.
  예: `홈 > 영웅 > 시에라`
- 시즌/패치처럼 시간축이 중요한 곳은 연도 메타를 보조 라벨로 병기한다.

### 관련 콘텐츠 모듈
- 모든 상세 페이지 하단에는 최소 3개의 관련 링크를 둔다.
- 우선순위는 `같은 축의 인접 문서 -> 영향받은 다른 축 -> 출처` 순서다.
- 이 모듈 덕분에 상세 페이지가 고립되지 않게 한다.

## 8. 섹션별 탐색 패턴
### 영웅
- 기본 탐색 수단은 역할 탭이다.
- 보조 필터는 서브롤, Stadium 가능 여부, 가나다순이다.
- 카드에서는 짧은 역할 정보만 보여 주고, 플레이스타일 설명은 상세로 넘긴다.

### 시즌
- 기본 탐색 수단은 연도 필터와 시즌 순서다.
- 홈에서는 최신 시즌 1개와 직전 시즌 1개만 강하게 노출한다.
- 시즌 상세에서는 연결된 패치와 신규 영웅을 함께 보여 줘야 한다.

### 패치
- 기본 탐색 수단은 최신순 리스트와 월별 그룹이다.
- 이벤트 일정 충돌이나 출처 차이는 카드 배지가 아니라 상세 페이지의 검증 상태 영역에서 분명히 설명한다.
- 패치 상세는 운영자가 사실 기준으로 삼는 기준 문서 역할을 한다.

### 모드
- `/modes`는 카드형 허브로 시작하되, `/modes/core-pvp`, `/modes/competitive`, `/modes/stadium` 세 갈래를 명확히 분리한다.
- Stadium은 별도 시스템처럼 느껴져야 하므로 코어 PvP와 같은 카드 밀도로 섞지 않는다.

### 맵
- 기본 탐색 수단은 모드 필터와 리워크 상태다.
- 맵 상세는 전략 팁보다 `어느 모드에 속하는지`, `무엇이 바뀌었는지`, `어떤 패치와 연결되는지`를 우선한다.

### 출처
- 공식 링크 목록만 나열하지 말고, `현재 기준 문서`, `충돌 중인 문서`, `다음 확인 예정`을 구분한다.
- 이 섹션은 신뢰 허브이지 시각 주연이 아니므로 레이아웃은 가장 차분해야 한다.

## 9. 페이지 템플릿 규칙
### 홈
- 가장 강한 포스터형 화면이다.
- 첫 뷰포트에는 `제품 정체성`, `현재 기준선`, `주요 진입 CTA`만 남긴다.
- 홈 안에서 각 지식 축은 요약 카드로만 맛보기 제공하고, 자세한 서술은 상세 페이지로 넘긴다.

### 인덱스 페이지
- 장식 강도는 홈보다 낮추고, 필터와 스캔 속도를 우선한다.
- 상단 1개 리드 카드와 아래 데이터형 카드 군으로 위계를 분리한다.
- 필터는 한 줄에서 끝내고, 필터 자체가 시각 주연이 되지 않게 한다.

### 상세 페이지
- 상단에는 한 줄 결론, 핵심 메타, 검증 상태를 먼저 보여 준다.
- 본문은 `현재 이해 -> 연결 변화 -> 주의 포인트 -> 공식 출처` 순서로 배치한다.
- 페이지마다 강한 강조 패널은 1개만 허용한다.

## 10. 공통 메타 정보
모든 상세 엔티티는 아래 메타를 공통으로 가진다.

| 필드 | 용도 |
| --- | --- |
| `title` | 페이지 제목 |
| `entityType` | hero / season / patch / mode / map / source |
| `summary` | 인덱스와 공유되는 1~2문장 요약 |
| `lastVerifiedAt` | 마지막 검증 절대 날짜 |
| `sourceIds` | 연결된 공식 출처 |
| `verificationState` | 검증 완료 / 워치리스트 / 출처 충돌 |
| `relatedEntityIds` | 관련 영웅, 시즌, 패치, 모드, 맵 |

검증 상태 노출 규칙
- `검증 완료`: 출처 충돌 없음
- `워치리스트`: 최신 패치나 이벤트 종료 등 재확인 필요
- `출처 충돌`: 공식 문서끼리 날짜나 표현이 다름

## 11. 중복 방지 규칙
- 홈은 상세의 축약본이지 복사본이 아니다.
- 인덱스 카드에는 긴 문단을 넣지 않는다.
- 한 사실의 가장 긴 설명은 단 하나의 상세 페이지에만 둔다.
- 다른 페이지에서는 요약 1~2문장과 링크만 제공한다.
- 시즌과 패치가 같은 변화를 다룰 때도, 시즌은 맥락을 설명하고 패치는 기준 날짜를 설명한다.

## 12. 반응형 규칙
- 모바일에서도 `현재 위치`, `상위 경로`, `핵심 CTA`를 첫 두 화면 안에서 확인할 수 있어야 한다.
- 브레드크럼은 모바일에서 한 줄 축약이 가능해야 한다.
- 필터는 가로 스크롤 허용, 다단 래핑 최소화 원칙으로 설계한다.
- 홈만 포스터형 긴장감을 강하게 유지하고, 인덱스와 상세는 읽기 우선으로 밀도를 줄인다.

## 13. 시각 적용 강도
| 화면 유형 | 시각 강도 | 비고 |
| --- | --- | --- |
| 홈 | 가장 강함 | 포스터형, 사선 프레임 적극 사용 |
| 섹션 인덱스 | 중간 | 장식보다 스캔 우선 |
| 상세 | 중간 이하 | 정보 리듬과 검증 메타 우선 |
| 출처 | 가장 낮음 | 신뢰 보강 역할 |

추가 원칙
- 내비게이션, CTA, 필터를 모두 같은 pill 형태로 수렴시키지 않는다.
- 홈 아래로 내려갈수록 프레임 장식은 줄이고 정보 계층은 더 선명하게 만든다.
- 상세 페이지에서도 오버워치풍 에너지는 남기되, 읽기 방해가 되면 즉시 줄인다.

## 14. 구현 우선순위 제안
### Wave 1
- 라우트 구조 추가
- 홈에서 각 섹션 인덱스로 분기
- 영웅/시즌/패치/모드/맵/출처 인덱스와 상세 템플릿 골격 구축
- 브레드크럼, 검증 상태 카드, 관련 콘텐츠 모듈 공통화

### Wave 2
- 맵 아카이브 확장
- 시즌과 패치 간 인접 문서 탐색 강화
- 워치리스트 자동 노출 규칙 정리

### Wave 3
- 전역 빠른 점프 또는 검색
- 더 깊은 비교형 탐색

## 15. UX QA 게이트
- 사용자는 홈에서 2클릭 이내에 영웅, 시즌, 패치, 모드, 맵, 출처 중 원하는 축에 들어갈 수 있어야 한다.
- 상세 페이지는 브레드크럼, 관련 링크, 출처 중 하나라도 빠지면 승인하지 않는다.
- 홈이 아카이브 카드 덩어리처럼 보이면 실패다.
- 인덱스가 다시 포스터처럼 과장되면 실패다.
- 출처 페이지가 다른 페이지보다 더 화려하면 실패다.
- 모바일에서 필터가 두 줄 이상 엉켜 현재 위치를 가리면 실패다.

## 16. 최종 판단
- 이번 v2의 핵심은 페이지 수 확대가 아니라 `탐색 가능한 구조`를 만드는 것이다.
- 따라서 먼저 필요한 것은 화려한 추가 카드가 아니라 `전역 내비게이션`, `섹션 인덱스`, `상세 템플릿`, `검증 상태 노출`, `관련 콘텐츠 연결`이다.
- 이 구조가 먼저 잠겨야 이후 시즌/패치/맵 아카이브가 커져도 사이트가 무너지지 않는다.
